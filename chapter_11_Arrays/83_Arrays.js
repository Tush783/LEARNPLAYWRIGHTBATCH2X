/* what is array? 

⠶ An array is a data structure that stores multiple values in a single variable. Think of it as a list where you can keep related items together.

  Key characteristics:

  - Ordered collection (items have positions 0, 1, 2, etc.)
  - Can hold any data type (numbers, strings, objects, even other arrays)
  - Dynamic size - can grow or shrink

  Example:

    let fruits = ["apple", "banana", "orange"];
    let numbers = [1, 2, 3, 4, 5];

    // Access items by index (starts at 0)
    console.log(fruits[0]); // "apple"
    console.log(fruits[1]); // "banana"

    // Common operations
    fruits.push("grape");    // Add to end
    fruits.pop();            // Remove from end
    fruits.length;           // Get size (3)
*/
let fruits = []; //Empty
let fruits_fresh = ["apple", "banana", "orange"];
//length = 3 index = 0,1,2

let arr = [10,20,30,40];
console.log(arr[0]);
console.log(arr[3]);
console.log(arr[4]);
